# ロードマップと受け入れ基準

合計約 14 週・8 Step。**各 Step の基準は `npm run gate:stepN` として自動化し、赤なら次 Step のブランチを切らない。** Step の順序を入れ替えず、後 Step の機能を前 Step に混ぜない。

| Step                     | 期間 | 成果物                                                                                                  | 受け入れ基準（`gate:stepN`）                                                                                                                | 状態                     |
| ------------------------ | ---- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| 0 設計・骨組み           | 1 週 | `docs/spec.md`（ユースケース 10 件・ER 図・API 一覧）、リポジトリ雛形、Docker Compose、CI               | `typecheck/lint/test` が CI で緑／OpenAPI 定義が存在し `npm run gen` で型生成が通る／ADR 3 件以上                                           | 実装済み（`gate:step0`） |
| 1 エージェント台帳・権限 | 2 週 | Agent/Tenant/Role の CRUD、API キー発行、RBAC（閲覧/実行/停止）                                         | ユニット＋API テスト 60 件以上 pass／権限違反テスト全パターン（役割 3 × 操作 3）で 403／`npm audit` high 0                                  | 実装済み（`gate:step1`） |
| 2 コスト計測プロキシ     | 2 週 | LLM 呼び出しを中継するプロキシ（Anthropic/OpenAI 互換）、トークン・料金を `UsageEvent` に記録、日次集計 | 料金計算の単体テストがベンダー公表単価と誤差 0／プロキシ経由の追加遅延 p95 ≦ 50ms（autocannon）／1 万件投入で集計 SQL ≦ 1 秒                | 実装済み（`gate:step2`） |
| 3 品質評価               | 2 週 | LLM-as-judge による応答採点（正確性・安全性・逸脱）、評価セット管理、回帰比較                           | 固定評価セット 100 件で採点の再現率 ≧ 90%／幻覚 ID など不正出力の除外テスト全 pass／評価失敗時のフォールバックがテストで保証                | 未着手                   |
| 4 ガードレール・停止     | 2 週 | しきい値ルール（コスト超過・品質低下・エラー率）、自動停止、通知（Webhook/メール）、監査ログ            | ルール発火から停止まで ≦ 3 秒（統合テスト）／監査ログの改ざん検知テスト pass／E2E で「登録→実行→超過→停止→復帰」が通る                      | 未着手                   |
| 5 ダッシュボード         | 2 週 | テナント別のコスト・品質・稼働率・インシデント一覧、日次レポート                                        | Lighthouse Performance/Accessibility ≧ 90／主要 5 画面の E2E 全 pass／表示データと DB 集計の突合テスト一致 100%                             | 未着手                   |
| 6 マルチテナント・課金   | 2 週 | Stripe 連携（Free/Pro/Enterprise）、機能ゲート、レート制限、テナント分離                                | テナント越境アクセステスト全パターンで拒否／Webhook 冪等性テスト pass／契約テスト（別 DB）が CI で緑／全体カバレッジ ≧ 80%                  | 未着手                   |
| 7 リリース準備           | 1 週 | Vercel/Supabase 向けデプロイ設定、README と API docs、デモシード、負荷試験レポート                      | クリーン環境で `docker compose up` → 5 分以内にデモ動作／同時 100 リクエストでエラー率 < 1%／`npm run build` 成功／既知バグ 0（Issue 管理） | 未着手                   |

## ゲートの運用ルール

1. `scripts/gate-stepN.mjs` が基準を順に検査し、1 つでも赤なら非 0 で終了する。
2. CI（`.github/workflows/ci.yml`）は実装済みの最新 Step のゲートを必ず実行する。
3. Step N+1 のブランチは、`main` で `gate:stepN` が緑になってから切る。
4. 基準を緩める変更は、このファイルと該当 ADR を同じ PR で更新する（テスト側だけを書き換えない）。

## 前提

- Next.js 16 + Prisma 7 + PostgreSQL 16（Docker）。
- 検証はすべてローカル＋CI で完結する（人手の営業・ヒアリングは含めない）。
