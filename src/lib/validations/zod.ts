// Zod の入口。既定メッセージを日本語にした z をここから配る (issues.message も他の文言と同じく日本語で返す)。
// スキーマを書くファイルは 'zod' ではなくこのモジュールから z を import する (読み込み順に依存せず必ずロケールが効く)
import { z } from 'zod';

// 既定メッセージを日本語にする (検証の意味は変わらず、文言だけが変わる)
z.config(z.locales.ja());

// 設定済みの z を再公開する
export { z };
